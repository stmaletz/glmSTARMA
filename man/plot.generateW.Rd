% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generateW.R
\name{plot.generateW}
\alias{plot.generateW}
\title{Plot Spatial Weight Matrices for Simulation}
\usage{
\method{plot}{generateW}(
  x,
  order = 1,
  type = c("matrix", "network"),
  col = NULL,
  node_col = "black",
  edge_col = "gray",
  ...
)
}
\arguments{
\item{x}{An object of class \code{generateW}, returned by the \code{generateW} function.}

\item{order}{(integer scalar) Specifies which spatial order of the neighborhood to plot. Default is \code{0}.}

\item{type}{(character scalar) Specifies the type of plot. Options are "matrix" for a heatmap representation of the weight matrix, and "network" for a network graph representation of the neighborhood structure. Default is "matrix".}

\item{col}{(character vector) Color palette for the heatmap. Default is a blue gradient. Ignored if \code{type} is "network".}

\item{node_col}{(character scalar) Color of the nodes in the network graph. Default is "black". Ignored if \code{type} is "matrix".}

\item{edge_col}{(character scalar) Color of the edges in the network graph. Default is "gray". Ignored if \code{type} is "matrix".}

\item{...}{Additional arguments passed to the underlying plotting functions.}
}
\description{
This function plots spatial weight matrices and neighborhoods generated by the \code{generateW} function.
}
\details{
Plotting neighborhood structures of type "independent" or "full" is not meaningful, as these structures do not represent spatial dependencies in a conventional sense.
For "independent", each location is treated as an isolated node, while "full" represents a complete graph where every location is connected to every other location, but each connection is treated independently.
}
\examples{
W_rect <- generateW(method = "rectangle", dim = 100, maxOrder = 2, width = 5)
plot(W_rect, order = 1, type = "matrix")
plot(W_rect, order = 1, type = "network")

}
