## Note
To be able to run the `sst.R` code the `STRbook` package must be installed from GitHub:  
```r
# install.packages("devtools")
devtools::install_github("andrewzm/STRbook")
```
Additionally the `Matrix` package is required:
```rr
install.packages("Matrix")
```